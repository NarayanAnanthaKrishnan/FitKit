# Use constraints.txt for the validated reproducible install:
# python -m pip install -c constraints.txt -r requirements.txt
fastapi>=0.115.0
uvicorn>=0.32.0
sqlalchemy>=2.0.0
asyncpg>=0.30.0
python-dotenv>=1.0.0
alembic>=1.13.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
